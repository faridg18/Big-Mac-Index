{
  "name": "big-mac",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare-client": "cd client && yarn install",
    "prepare-server": "cd server && yarn install",
    "prepare": "yarn prepare-client && yarn prepare-server ",
    "client": "cd client && yarn start",
    "server": "cd server && yarn start",
    "start": "concurrently \"yarn client\" \"yarn server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.1"
  }
}
